/**
 * Public API types.
 *
 * This barrel file re-exports all shared API-related TypeScript types
 * to provide a clean and stable import surface for consumers.
 *
 * Usage:
 *   import type { IPet, IDataResponse } from '@/api/types';
 */
export * from './api.types';